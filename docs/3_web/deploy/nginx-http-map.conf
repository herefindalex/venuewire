# Include ONCE in nginx http {} context, NOT inside server {} or location {}.
# Suggested installed name: /etc/nginx/conf.d/trading-console-map.conf
map $http_upgrade $console_connection_upgrade {
    default upgrade;
    ''      close;
}
